version: "3"

services:
  nginx:
    restart: always

  web:
    restart: always

  worker_general:
    restart: always

  scheduler:
    restart: always

  postgres:
    restart: always

  rabbitmq:
    restart: always
